// Shows performance stats (e.g., returns).
const PerformanceMetrics = () => {
    return (
        <div>
            <h2>Performance Metrics</h2>
        </div>
    );
};

export default PerformanceMetrics; // ✅ Ensure 'default' export
